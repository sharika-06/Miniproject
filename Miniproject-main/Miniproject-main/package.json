{
  "name": "neurograph-unified",
  "version": "1.0.0",
  "description": "Unified NeuroGraph deployment",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "render-build": "cd frontend && npm install && npm run build && cd ../backend && npm install && cd ../../../admin_portal/frontend && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-node": "^1.2.0",
    "isomorphic-git": "^1.37.4"
  }
}
